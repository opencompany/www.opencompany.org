language: ruby
env:
  global:
    - secure: "AAAAB3NzaC1yc2EAAAADAQABAAAAgQD7TPrbXg1nN7JI7veuhj/KWBP3r8i5YdaU8phrUm+Qyo2N+exCP45XbB2MxwBHgSsZK5XI4H74+9mKZURqVuRS5ntjunoOz3HxlfW2tckIJ26vfzJ/P2CZy3cH2PF2MyHIR/oBm3IWm9ZxvFCjTX/GYIBMxH+8Q6UVfHqRu5FQnQ="
rvm:
  - 2.1.0
script: bundle exec jekyll build
branches:
  only:
    - master
before_script:
  - echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
after_success:
  - rm -rf _deploy
  - mkdir _deploy
  - cd _deploy
  - git clone https://github.com/opencompany/opencompany.github.io.git --branch=master .
  - rsync -avzh ../_site/ ./
  - git add .
  - git config --global user.name "$GIT_NAME"
  - git config --global user.email "$GIT_EMAIL"
  - git commit -m "New site version. Committed by Travis-CI."
  - git config credential.helper "store --file=.git/credentials"
  - echo "https://${GH_TOKEN}:@github.com" > .git/credentials
  - git push origin master
notifications:
  irc:
    on_success: change
    on_failure: change
    channels:
    - irc.freenode.net##opencompany
    template:
    - '%{repository}#%{build_number} (%{branch}) %{message} %{build_url}'
